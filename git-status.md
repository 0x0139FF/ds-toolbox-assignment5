# Definition: git status

The `git status` command show current state of working directory and staging area. It list tracked, untracked, modified, and staged files, helping see change and decide what commit next.

In Jupyter Lab, use **Git extension** in sidebar. It show current branch, modified files, and staged change, allow managing Git workflow visually.